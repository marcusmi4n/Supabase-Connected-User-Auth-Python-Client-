# Supabase User Auth (Python)

## Description
Python wrapper functions to register and log in a user using Supabase’s Auth API.

## Setup
1. Install: `pip install supabase`
2. Set up project URL and anon key